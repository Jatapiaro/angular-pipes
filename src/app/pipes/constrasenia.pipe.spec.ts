import { ConstraseniaPipe } from './constrasenia.pipe';

describe('ConstraseniaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConstraseniaPipe();
    expect(pipe).toBeTruthy();
  });
});
